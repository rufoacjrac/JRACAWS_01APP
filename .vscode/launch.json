{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/jracaws_01app/package.json",
                "localRoot": "${workspaceFolder}/jracaws_01app"
            }
        }
    ]
}